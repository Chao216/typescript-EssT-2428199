let fulll: Record<number, string | number | boolean> = { 12: "Hello world" }; //    key must be number, values could be string, number or boolean
console.log(fulll);

fulll = { 12: "james bond" };

console.log(fulll);
